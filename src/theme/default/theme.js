import colors from './colors';

export default {
  palette: {
    primary: {
      main: colors.pink[500],
      light: colors.pink[300]
    },
    secondary: {
      light: colors.grey,
      main: colors.grey,
      dark: colors.grey
    }
  }
};
